import { SoyunadirectivaDirective } from './soyunadirectiva.directive';

describe('SoyunadirectivaDirective', () => {
  it('should create an instance', () => {
    const directive = new SoyunadirectivaDirective();
    expect(directive).toBeTruthy();
  });
});
